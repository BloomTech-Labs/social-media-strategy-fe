const initialState = {};

const listsReducer = (state = initialState, action) => {
  const newState = { ...state };
  switch (action.type) {
    default:
      break;
  }
  return newState;
};

export default listsReducer;
