/*describe("create a new so me account", function() {
  it("visits the site, clicks sign up, provides an email and password, then logs in", function() {
    cy.visit("/login");
    cy.get("[data-cy=registerButton]").click();
    cy.get("[data-cy=email]")
      .click()
      .type("cypressTest@cypress.com");
    cy.get("[data-cy=password]")
      .click()
      .type("cypress");
    cy.get("[data-cy=submit]").click();
  });
});
*/