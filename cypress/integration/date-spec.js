describe('it tests the functionality of the navigation', function() {
  // it('allows you to login with valid credentials', () => {
  //   cy.visit('/login');
  //   cy.get('[data-cy=email]').type('test@test.com');
  //   cy.get('[data-cy=password]').type('test');
  //   cy.get('[data-cy=submit]').click();
  //   cy.wait(300);
  //   cy.location('pathname').should('eq', '/home/accounts');
  // });
  // it('checks to see if the right date is being rendered', function() {
  //   const todaysDate = Cypress.moment().format('MMM DD, YYYY');
  //   cy.contains(todaysDate);
  // });
  // it('checks to see if the right day is being rendered', function() {
  //   const day = Cypress.moment().format('dddd');
  //   cy.contains(day);
  // });
  // it('checks to see if the right time is being rendered', function() {
  //   const time = Cypress.moment().format('LT');
  //   cy.contains(time);
  // });
});
